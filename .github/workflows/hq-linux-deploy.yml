name: HQ Linux deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: [self-hosted, linux, hq]
    steps:
      - name: Pull and restart kcw-api units
        run: bash "$HOME/projects/kcw-api/scripts/hq-linux-deploy.sh"
